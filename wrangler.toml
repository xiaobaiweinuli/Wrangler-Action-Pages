# CF Deploy — Cloudflare Pages 部署配置
# 将此网页部署为 Cloudflare Pages 静态站点
#
# 使用方式（二选一）：
#   1. 推送到 GitHub，在 Cloudflare Dashboard 连接仓库自动部署
#   2. 在本机运行：wrangler pages deploy .

name = "cf-deploy"
pages_build_output_dir = "."
compatibility_date = "2026-03-10"
